import { PageHeader } from "@/components/PageHeader";

export default function WatchlistPage() {
  return (
    <PageHeader eyebrow="Watchlist" title="Alerts for score and evidence changes.">
      Watchlists will notify users when evidence, score snapshots, corrections,
      disputes, reports, or availability change.
    </PageHeader>
  );
}

