import { PageHeader } from "@/components/PageHeader";

export default function SavedPage() {
  return (
    <PageHeader eyebrow="Saved" title="Saved products, businesses, and reports.">
      Saved lists will support Plus subscriptions, household profiles, shopping
      comparison, watchlists, and report follow-up. No saved action changes rank.
    </PageHeader>
  );
}

