import { PageHeader } from "@/components/PageHeader";

export default function AdminPricingPage() {
  return (
    <PageHeader eyebrow="Admin pricing" title="Configurable prices by market and surface.">
      Pricing records will be configurable by product surface, plan, country,
      income tier, currency, interval, sponsored access, and active status.
    </PageHeader>
  );
}

