import { PageHeader } from "@/components/PageHeader";

export default function AdminCategoriesPage() {
  return (
    <PageHeader eyebrow="Admin categories" title="Category mapping without seeded scores.">
      Categories will guide evidence requirements, product discovery, local
      search, and supplier/seller matching. Real scores require real evidence.
    </PageHeader>
  );
}

